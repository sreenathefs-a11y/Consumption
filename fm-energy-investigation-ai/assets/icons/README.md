# Icons
The PWA currently uses the code-native lightning mark so there are no unavailable image assets. Add generated 192px and 512px PNG icons here before app-store packaging and list them in `manifest.json`.
