import { initShell,statusBadge } from './ui.js';initShell();document.querySelector('#statusGuide').innerHTML=[['Normal / verified / completed','Green'],['Review required','Yellow'],['High priority / abnormal','Orange'],['Critical / data issue','Red'],['In progress','Blue'],['Missing / not checked / unavailable','Grey']].map(([label,color])=>`<div>${statusBadge(label)}<span>${color} · ${label}</span></div>`).join('');
