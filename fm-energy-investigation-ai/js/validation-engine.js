export function validateMeter(m,meters=[]){const e=[];if(!m.number?.trim())e.push('Meter number is required.');if(meters.some(x=>x.number===m.number&&x.id!==m.id))e.push('This meter number already exists.');if(Number(m.tariff)<0)e.push('Tariff cannot be negative.');if(m.ctRatio&&!/^\d+(\.\d+)?\/\d+(\.\d+)?$/.test(m.ctRatio))e.push('CT ratio must use a format such as 200/5.');return e}export function canClose(c){const e=[];if(!c.rootCause)e.push('Confirm an evidence-supported root cause before closure.');if(!c.evidence.length)e.push('Supporting evidence is required.');if(c.actions.some(a=>a.status!=='Verified'))e.push('Every corrective action must be verified.');return e}export function validateReading(open,close){return Number(close)<Number(open)?['Closing reading cannot be below opening reading without a documented reset.']:[]}
