import {storage} from './storage.js';import {initShell,toast} from './ui.js';import {download} from './utils.js';initShell();const form=document.querySelector('#settingsForm');if(form){const s=storage.get().settings;Object.entries(s).forEach(([k,v])=>{if(form.elements[k])form.elements[k].value=v});form.addEventListener('submit',e=>{e.preventDefault();const o=Object.fromEntries(new FormData(form));['defaultTariff','warningThreshold','criticalThreshold','evidenceWarningKb'].forEach(k=>o[k]=Number(o[k]));storage.mutate(db=>Object.assign(db.settings,o));toast('Settings saved');setTimeout(()=>location.reload(),400)});document.querySelector('#export').onclick=()=>download('fm-energy-backup.json',storage.export());document.querySelector('#import').onchange=async e=>{try{storage.import(await e.target.files[0].text());toast('Backup imported');setTimeout(()=>location.reload(),500)}catch(x){toast(x.message)}};document.querySelector('#reset').onclick=()=>{if(confirm('Reset all local data to the demo baseline?')){storage.reset();location.reload()}}}
